import styled from 'styled-components'
import { theme } from '../../globalStyle'

export const StyledMessageToRepeat = styled.p`
  font: 600 22px/26px ${theme.font.primary};
  text-align: center;
  padding: 0 20%;
  margin-top: 25px;
  color: ${theme.color.dark.main};
`
